// Write a javascript function which accepts an argument and returns the type 
// Note: there are six possible values that typeof returns: object, boolean, function, number, string, and undefine

function typeTeller(elem){
    return typeof elem;
}

console.log(typeTeller(10));